<br>
<center><h5>Jurnal Membaca Tahap 2</h5></center>
<div style="float: right">
  <%= link_to 'Add Data', new_tr_jurnal2_path, class: 'btn btn-success' %>
</div>
<table class="table table-bordered">
  <thead class="thead-dark">
    <tr>
      <th scope="col">No</th>
      <th scope="col">Tanggal</th>
      <th scope="col">Judul </th>
      <th scope="col">Pengarang </th>
      <th scope="col">Resume</th>
      <% if current_ms_user.role == "2" %>
      <th scope="col">Nama</th>
      <% end %>
      <th scope="col">Status</th>
      <th scope="col" width="300">Action</th>
    </tr>
  </thead>
   <% if current_ms_user.role == "1" %>
<% @tr_jurnal2s.each_with_index do |tr_jurnal2, index|%>
<tbody>
<tr>
      <th scope="row"><%= index + 1 %></th>
      <td> <%= tr_jurnal2.tanggal %> </td>
      <td> <%= tr_jurnal2.judul %> </td>
      <td> <%= tr_jurnal2.pengarang %> </td>
      <td> <%= tr_jurnal2.resume %> </td>
      <td> <%= tr_jurnal2.status %> </td>
     <td class="text-center">
        <%= link_to 'Delete',tr_jurnal2_path(tr_jurnal2), method: :delete, data: {confirm: 'Delete Data ? '}, class: 'btn btn-danger' %>
              <%= link_to 'Detail', tr_jurnal2_path(tr_jurnal2), class: 'btn btn-light' %>
              <%= link_to 'Edit', edit_tr_jurnal2_path(tr_jurnal2), class: 'btn btn-primary' %>
              
            </td>
     
</tbody>
</tr>
 <% end %>
  <% end %>
   <% if current_ms_user.role == "2" %>
<% @tr_jurnal2.each_with_index do |tr_jurnal2, index|%>
<tbody>
<tr>
      <th scope="row"><%= index + 1 %></th>
      <td> <%= tr_jurnal2.tanggal %> </td>
      <td> <%= tr_jurnal2.judul %> </td>
      <td> <%= tr_jurnal2.pengarang %> </td>
      <td> <%= tr_jurnal2.resume %> </td>
      <td> <%= tr_jurnal2.nama %> </td>
      <td> <%= tr_jurnal2.status %> </td>
     <td class="text-center">
        <%= link_to 'Setuju',activate_jurnal2_path(id:tr_jurnal2.id), class: 'btn btn-primary', method: :patch%> 
      </td>
     
</tbody>
</tr>
 <% end %>
  <% end %>